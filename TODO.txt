Remove shared_ptr<base_connection> and general connection/service memory management model.
any_invocable type over std::function.
Upgrade to C++20.
Look into using std::span where applicable.
Inspect buffer architecture to make sure we're still doing things efficiently.
Remove DH_new/DH_free (deprecated).
